import 'package:flutter/material.dart';

class EditDiscardDialog extends StatelessWidget {
  const EditDiscardDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AlertDialog(
      actions: [],
    );
  }
}
